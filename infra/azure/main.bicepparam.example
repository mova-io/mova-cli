// Example parameter file for infra/azure/main.bicep.
//
// COPY this to a NON-committed file (e.g. main.dev.bicepparam) before
// deploying. The .gitignore at the repo root excludes
// *.bicepparam EXCEPT main.bicepparam.example.
//
// To deploy:
//   1. Pre-create the resource group:
//        az group create -n movate-dev-rg -l eastus2
//   2. Pre-populate Key Vault secrets (chicken-and-egg note below).
//   3. Run the deployment with this param file:
//        az deployment group create \
//            -g movate-dev-rg \
//            -f infra/azure/main.bicep \
//            -p main.dev.bicepparam
//
// Chicken-and-egg note: the deployment provisions Key Vault, but the
// Container Apps reference secrets that must exist in KV at deploy
// time. Two options:
//   A. Two-pass deploy: first pass creates KV + Postgres + ACR + Logs;
//      operator populates secrets; second pass deploys the Container
//      Apps. Cleaner but more steps.
//   B. Pre-create KV by hand, then run the full deploy (Bicep is
//      idempotent on KV).
// Stage 2 (`movate deploy` CLI) automates option A.

using './main.bicep'

// REQUIRED — these have no defaults.
param env = 'dev'
param image = 'movate:0.5.0'

// First-pass deploys (env never deployed before): set to FALSE so the
// Container Apps don't try to resolve KV secrets that don't exist yet.
// Provision infra → populate KV secrets → flip this to TRUE → re-run
// the deployment. After that, leave it true.
param enableApiWorker = false

// REQUIRED — sourced from Key Vault using `getSecret`. Replace the
// vault name + secret name with your real values. Or for a one-off
// dev deploy you can pass a literal; just NEVER commit a literal.
//
// Example using a Key Vault reference (recommended):
//
//   param postgresAdminPassword = az.getSecret(
//     '<subscriptionId>',
//     'movate-bootstrap-rg',     // RG that holds the bootstrap KV
//     'movate-bootstrap-kv',     // name of the bootstrap KV
//     'pg-admin-password'        // secret name
//   )
//
// Replace this dummy literal before deploying for real:
param postgresAdminPassword = 'ReplaceMeBeforeDeployment-ChangeMe!'

// OPTIONAL — defaults to resourceGroup().location if omitted.
param location = 'eastus2'

// OPTIONAL but STRONGLY RECOMMENDED — short suffix (2-8 alphanumeric
// chars) appended to globally-unique resource names. Key Vault, ACR,
// Postgres Flex, and ACS resources live in Azure-wide namespaces, so
// vanilla `movate-dev-kv` etc. are likely already claimed by another
// tenant. Pick an org slug or your initials. RG-scoped resources (logs,
// ACA env, ACA apps) ignore this — they only have to be RG-unique.
//
// Example: nameSuffix = 'mvio' → KV becomes 'movate-dev-kv-mvio',
// ACR becomes 'movatedevacrmvio'.
param nameSuffix = ''

// OPTIONAL — common tags. Add your own as needed (cost center, owner, ...).
param tags = {
  application: 'movate'
  environment: 'dev'
  managedBy: 'bicep'
  owner: 'platform-team'
}

// OPTIONAL — provision Azure Communication Services for outbound SMS
// notifications. Off by default; flip to `true` ONLY when you're
// ready to A2P-10DLC-register the brand (~2-3 weeks ops work). See
// docs/v1.0-azure-design.md §10 for the decision rationale and
// docs/azure-bootstrap.md for the post-provision runbook.
//
//   param enableSms = true
//   param acsFromNumber = '+18885551234'  // E.164, from `az communication phonenumber purchase`
//
// Leave `acsFromNumber` empty on the provisioning pass: the ACS
// resource lands first, operator buys the toll-free number from the
// Azure Portal or `az communication phonenumber purchase`, then sets
// this param and redeploys. The worker only gets the
// MOVATE_ACS_* env vars when BOTH `enableSms` is true AND
// `acsFromNumber` is non-empty.
param enableSms = false
param acsFromNumber = ''

// OPTIONAL — Telegram operator alerts. Pings you on every terminal job
// via a Telegram bot. Free, no regulatory hurdles, ~5 min setup. Two
// steps after flipping enableTelegram = true:
//   1. @BotFather → /newbot → save the token (~30s)
//   2. /start your bot, then visit
//      https://api.telegram.org/bot<TOKEN>/getUpdates → grab `chat.id`
//      (numeric) and set `telegramChatId` below
// Operator pastes the token into KV once:
//   az keyvault secret set --vault-name <kv> --name telegram-bot-token \
//       --value "<token>" > /dev/null
// Setup runbook in docs/azure-bootstrap.md → "Optional: Telegram alerts".
// The worker gets MOVATE_TELEGRAM_* env vars only when BOTH
// `enableTelegram` is true AND `telegramChatId` is non-empty.
param enableTelegram = false
param telegramChatId = ''
