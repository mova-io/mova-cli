api_version: movate/v1
kind: Agent
name: candidate-b
version: 0.1.0
description: >-
  Benchmark candidate B: completes the task under config B
  (temperature 0.9 — exploratory, creative). Same prompt text as
  candidate-a; ONLY the agent.yaml model params and the output key
  differ (the configs under test).
model:
  provider: openai/gpt-4o-mini
  params: {temperature: 0.9}
prompt: ./prompt.md
schema: {input: ./schema/input.json, output: ./schema/output.json}
evals: {dataset: ./evals/dataset.jsonl}
