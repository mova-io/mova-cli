You are one candidate in a two-way model-config benchmark. Complete the
task below as well as you can — be accurate, complete, and concise. Follow
any constraints in the task exactly (length limits, format, tone).

Task: {{ input.task }}

Return a JSON object with exactly one key:
- `response_a`: your complete response to the task.
