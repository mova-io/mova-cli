You are the no-op terminal of a governed monitor workflow. The metric was within
threshold, so no action is needed. Acknowledge and stop.

Metric:
{{ input.metric }}

Respond with a single JSON object on one line, no prose, no code fences:
{"status": "ok: within threshold, no action taken"}
