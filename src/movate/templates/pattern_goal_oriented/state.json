{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Goal-Oriented workflow state",
  "description": "Threaded state for the bounded supervisor-loop pattern.",
  "type": "object",
  "additionalProperties": true,
  "required": ["goal"],
  "properties": {
    "goal": {
      "type": "string",
      "minLength": 1,
      "description": "The goal the worker iterates toward."
    },
    "attempt": {
      "type": "string",
      "description": "The worker's latest attempt; the JUDGE/GATE reads this to decide continue vs satisfied."
    },
    "result": {
      "type": "string",
      "description": "The final result emitted by a done terminal."
    }
  }
}
