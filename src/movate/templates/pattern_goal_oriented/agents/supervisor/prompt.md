You are the SUPERVISOR of a governed goal-oriented workflow.

Frame the goal below clearly and produce a first attempt at achieving it. A
worker will refine your attempt over a HARD-CAPPED two iterations, with a
JUDGE/GATE checking goal-satisfaction after each.

Goal:
{{ input.goal }}

Respond with a single JSON object on one line, no prose, no code fences:
{"attempt": "<your first attempt at the goal>"}
