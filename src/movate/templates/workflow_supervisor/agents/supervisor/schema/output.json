{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Supervisor Node Output",
  "type": "object",
  "properties": {
    "category": {
      "type": "string",
      "enum": ["billing", "tech", "general"],
      "description": "Classified intent category for routing."
    }
  },
  "required": ["category"]
}
