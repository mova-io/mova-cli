{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Supervisor Workflow State",
  "description": "Shared state threaded across the supervisor -> specialist agent routing pipeline.",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "The user's original support query."
    },
    "category": {
      "type": "string",
      "enum": ["billing", "tech", "general"],
      "description": "Intent category assigned by the supervisor agent."
    },
    "response": {
      "type": "string",
      "description": "The specialist agent's response to the user's query."
    },
    "resolved": {
      "type": "boolean",
      "description": "Whether the query was fully resolved by the specialist.",
      "default": false
    }
  },
  "required": ["query"],
  "additionalProperties": false
}
