{"input": {"diff": "--- a/auth.py\n+++ b/auth.py\n@@ -10,7 +10,7 @@\n def check_password(user, password):\n-    if user.password_hash == hash_password(password):\n+    if user.password_hash == password:\n         return True\n     return False\n", "language": "python"}, "expected": {"summary": "Critical: the new code compares the stored hash against the raw plaintext password instead of hashing the candidate. This breaks every login.", "verdict": "request_changes", "findings": [{"file": "auth.py", "line": 12, "severity": "blocker", "category": "security", "message": "Comparing stored password_hash directly to the raw password — drops the hash function entirely. No user will be able to log in, and any equality check against a plaintext password is itself a vulnerability.", "suggestion": "Revert to `hash_password(password)` on the right-hand side."}]}}
{"input": {"diff": "--- a/utils.py\n+++ b/utils.py\n@@ -5,3 +5,6 @@\n def add(a, b):\n     return a + b\n+\n+def unused():\n+    pass\n", "language": "python"}, "expected": {"summary": "Adds an unused helper. Low priority — flag for cleanup.", "verdict": "comment", "findings": [{"file": "utils.py", "line": 8, "severity": "minor", "category": "maintainability", "message": "`unused()` has no callers and no body — dead code.", "suggestion": "Remove the function or implement and use it."}]}}
