# ADR 028 — discoverability metadata for the ReAct loop template.
title: ReAct loop (reason-act-observe cycle)
description: LangGraph-native ReAct pattern with cycle edges — a reasoner thinks step-by-step, delegates to a researcher when needed, and loops until the answer is sufficient.
shape: workflow
tags:
  - workflow
  - react
  - langgraph
  - cycle
  - tool-use
recommended_for: When your agent needs to reason iteratively, decide whether to gather more information, and loop until confident — the classic ReAct pattern.
