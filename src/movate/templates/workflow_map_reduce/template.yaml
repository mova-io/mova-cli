# ADR 028 — discoverability metadata for the map-reduce template.
title: Map-reduce (parallel fan-out / fan-in)
description: LangGraph parallel fan-out to analyst agents, fan-in merge — demonstrates parallel execution with sub-question decomposition.
shape: workflow
tags:
  - workflow
  - map-reduce
  - langgraph
  - parallel
  - fan-out
recommended_for: When your task can be decomposed into independent sub-questions that are answered in parallel and then merged into a single cohesive answer.
