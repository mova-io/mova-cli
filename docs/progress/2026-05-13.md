# Daily progress — 2026-05-13

End-of-day summary of today's shipped + in-flight work.

## TL;DR

Six PRs, four merged + two open. ADR 002 (Skills + Contexts) **fully closed**. ADR 003 (Teams integration) **drafted, accepted-pending-open-questions, slice 3.1.a (skeleton) opened**. Net new tests: ~80. Full suite: **1163 → 1200** passing.

| PR | Title | Status |
|---|---|---|
| [#58](https://github.com/mova-io/mova-cli/pull/58) | Skills PR 7 — `SkillPolicy` gate on `side_effects` categories | ✅ merged |
| [#59](https://github.com/mova-io/mova-cli/pull/59) | Four-dimension eval reporting (accuracy / faithfulness / coverage / latency) | ✅ merged |
| [#60](https://github.com/mova-io/mova-cli/pull/60) | ADR 003 — Microsoft Teams as a self-serve front door | ✅ merged |
| [#61](https://github.com/mova-io/mova-cli/pull/61) | Skills PR 6a — native Anthropic tool-use | ✅ merged |
| [#62](https://github.com/mova-io/mova-cli/pull/62) | Skills PR 6b — native OpenAI tool-use | ✅ merged |
| [#63](https://github.com/mova-io/mova-cli/pull/63) | Teams Slice 3.1.a — Bot Framework webhook skeleton | 🟡 open |

## Deva-driven priority order ran:

1. **Tier 1 — sales-demo path**: ADR 003 drafted, Slice 3.1.a opened. Blocked downstream on Azure-access email (user is sending).
2. **Tier 2 — roadmap completion**: ADR 002 closed via 6a + 6b. Side-effects policy gate (#58) hardens the skill story for prospects.
3. **Tier 3 — newly unblocked by canonical config split (yesterday)**: Untouched today; queued for next sessions.

## Per-PR detail

### #58 — Skills PR 7: side_effects policy gates

- New `SkillPolicy` model in `core/config.py` with `allowed_side_effects: list[SkillSideEffects] | None`
- Enforcement at executor entry (after runtime_policy check) AND at `mdk validate` time (belt-and-braces — operators see policy violations before any execute attempt)
- Wired into `cli/_runtime.py` via `project_cfg.skills`
- 16 tests in `test_skill_policy.py`: model unit tests + CLI integration + executor integration
- README section: "Skill policy — gate side_effects categories"

### #59 — Four-dimension eval reporting

- `Dimension` StrEnum, `DimensionScore` / `DimensionScores` / `DimensionalMeans` dataclasses
- New optional dataset fields: `grounding`, `expected_coverage`, `latency_budget_ms` (type-validated at load)
- Engine refactored with `_score_dimensions` orchestrator + per-dim methods
- `_FAITHFULNESS_PROMPT` template for the LLM-judge faithfulness dim
- Per-dim helpers: `_score_coverage` (deterministic substring), `_score_latency` (1.0 in budget → 0.0 at 2× budget)
- `_compute_dimensional_means` rollup helper
- **Back-compat preserved**: `CaseRun.score` (the gate input) stays accuracy-only. `--gate 0.7` still means "70% accuracy".
- Reporter updates: Rich table breakdown, JSON `dimensional_means` + `dimensions_per_run`, markdown breakdown — all opt-in (silent on legacy datasets)
- 34 new tests in `test_eval_dimensions.py` + 2 in `test_reporters.py`
- README section: "Four-dimension scoring (v0.6+)"

### #60 — ADR 003: Microsoft Teams as self-serve front door

- 360-line design doc capturing CEO ask: sales team should do the first demo themselves
- Three slices: 3.1 run-existing → 3.2 eval-with-upload → 3.3 saved-configs
- Cardinal architectural rule: Teams is a *client* of the v0.5 HTTP runtime. No new execution surface, no forked storage.
- Auth model: bot-fleet API key + per-user Movate API keys bound via `/movate connect` DM
- Status: **Proposed** (not yet Accepted; 4 open questions called out — multi-tenant prospects, Langfuse trace linking, mobile UX, streaming)
- v0.7 planned section added to README

### #61 — Skills PR 6a: native Anthropic tool-use

- `to_tool_spec` override emitting Anthropic's flat `{name, description, input_schema}` shape
- `complete()` accepts `tools=` and parses `tool_use` content blocks → `CompletionResponse(kind="tool_use", ...)`
- New `_translate_messages` helper: folds executor's OpenAI-style history into Anthropic content blocks
  - Assistant `tool_calls` → `tool_use` blocks (with text prelude preserved)
  - `role="tool"` results → user messages with `tool_result` blocks (consecutive results coalesced into one user message)
- First-wins on parallel tool_use blocks (matches LiteLLM PR 1 decision; parallel-dispatch is a follow-up)
- 11 new unit tests + 1 end-to-end integration test (real Executor + scripted FakeClient + real Python skill)
- README "Tool-use across runtimes" subsection

### #62 — Skills PR 6b: native OpenAI tool-use

- Much smaller diff than 6a — OpenAI SDK accepts the same flat-message + nested-tool-spec format the executor builds in. No translation needed.
- `tools=` passthrough in `complete()` (explicit-only; no empty arrays for Azure-OpenAI proxy compatibility)
- `_to_completion_response` walks `choices[0].message.tool_calls`; first-wins
- New `_func_field` helper accepts attribute-style OR dict-shaped tool_calls (SDK shape has shifted between versions)
- 7 new unit tests + 1 end-to-end integration test
- **Closed ADR 002** — every skill backend (Python/HTTP/MCP) now runs under every runtime (LiteLLM/native_anthropic/native_openai)

### #63 — Teams Slice 3.1.a: Bot Framework webhook skeleton

- New `src/movate/teams_bot/` package (optional, gated behind `[teams]` extra: FastAPI + uvicorn)
  - `activity.py` — hand-rolled Pydantic models for Activity, ChannelAccount, ConversationAccount, Mention, ReplyActivity. Camel-case aliases via Pydantic alias support.
  - `parser.py` — strips mention markup, tokenises `@movate <command> <args>`. Handles `help` / `ping` / `run`. Malformed args produce non-empty `parse_error` (never raise).
  - `handler.py` — async dispatch from Activity to ReplyActivity. Plain-text replies; ready to swap for Adaptive Cards in 3.1.b.
  - `app.py` — `build_app()` constructs FastAPI app with `GET /health` + `POST /api/messages`
- New CLI: `mdk teams-bot serve` (port 3978 default, matches Bot Framework Emulator)
- **Architecture call**: hand-roll the Bot Framework protocol — no `botbuilder-core` SDK. The SDK is ~30MB of azure-core transitive deps; we don't need JWT validation in the local-dev skeleton. Hardening PR lands separately before public exposure.
- 28 new tests: alias loading, mention stripping, every parser path, handler responses, end-to-end HTTP via TestClient, Unicode preservation
- README Quickstart subsection with 3-terminal local-dev flow

## Stats

| Metric | Before | After |
|---|---|---|
| Open PRs | 2 (#54, #55) | 1 (#63) |
| Merged PRs today | 0 | 5 (#58, #59, #60, #61, #62) |
| Full test suite | 1163 passing | 1200 passing |
| Tests added today | — | **~80 net new** |
| ADRs accepted | ADR 001, ADR 002 (open Qs) | ADR 002 closed, ADR 003 proposed |

## Smoke check: `mdk jobs show` (feature verification ask)

User asked whether **"Inspect jobs on a deployed runtime: `mdk jobs show`"** is working. **Verdict: ✅ yes, fully functional.**

- Implementation: `src/movate/cli/jobs.py:45` — Typer command, hits `MovateClient.get_job(job_id)` via HTTP
- Subcommands: `show`, `wait`, `list`, `list-agents`
- Tests passing: 8 in `tests/test_client_and_remote_cli.py` — happy path, unknown-id 404, list filter, list-agents
- CLI smoke: `uv run mdk jobs show --help` renders correctly with `--target`, `--output table|json` flags
- Exit codes: 0 for queued/success, 1 for terminal-but-failed (`ERROR`, `SAFETY_BLOCKED`) — lets bash distinguish in-flight from failed

## Blockers / unresolved

- **Azure access migration email** — drafted yesterday, user is sending to Movate IT. Critical-path unblock for Teams Slice 3.1.e (manifest upload + Bot Service registration) and the broader Phase 6 deploy.
- **Worktree hygiene** — `my-sandisk-agent/` + `openai-feedback.md` still untracked at repo root. Low priority; flagging.
- **ADR 003 open questions** — multi-tenant prospects, Langfuse trace linking across tenants, mobile UX verification, streaming. None block 3.1.a; will be resolved as later slices land.

## Next-session priorities (in rank order)

1. **Teams Slice 3.1.b** — `MovateClient.submit_and_wait` integration + Adaptive Card builders for `run_result` / `error`. Demo-visible. ~half day.
2. **Helm chart for the runtime** — operational win, unblocks non-Azure customers. ~2 days.
3. **Skill-using templates** in `templates/` — every `mdk init` includes a working skill so demos are immediate. ~1 day.
4. **`mdk bench` support for native runtimes** — close the gap so bench works against `native_anthropic` / `native_openai` provider strings. ~1 day.
5. **Parallel tool-use** — modern models emit parallel calls; currently first-wins. ~2 days incl. executor changes.

## Tier 3 (deferred, awaiting design)

- pgvector memory tiers — Tier 3 RAG opener; needs ADR 004 first
- HITL workflow nodes — no demo pressure; complex design
- Apache AGE knowledge graph + d3.js viz — flashy but expensive
- SaaS onboarding flow — needs design
