api_version: movate/v1
kind: Agent
name: notify
version: 0.1.0
description: >-
  Writes the refresh status update for the KB operators — the shared tail
  both the published path and the acknowledged-failure path converge on.
model:
  provider: openai/gpt-4o-mini
  params: {temperature: 0.0}
prompt: ./prompt.md
schema: {input: ./schema/input.json, output: ./schema/output.json}
evals: {dataset: ./evals/dataset.jsonl}
