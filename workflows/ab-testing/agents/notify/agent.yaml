api_version: movate/v1
kind: Agent
name: notify
version: 0.1.0
description: >-
  Confirms the experiment increment: which variant served the request
  and that the outcome was recorded (with the ledger reference).
model:
  provider: openai/gpt-4o-mini
  params: {temperature: 0.0}
prompt: ./prompt.md
schema: {input: ./schema/input.json, output: ./schema/output.json}
evals: {dataset: ./evals/dataset.jsonl}
